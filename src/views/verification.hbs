<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta http-equiv='X-UA-Compatible' content='IE=edge' />
    <meta
      name='viewport'
      content='width=device-width, initial-scale=1.0'
    />
    <title>
      Verify Your Email
    </title>
    <style>

      body {
        background: #f4f6fb;
        font-family: 'Segoe UI', Arial, sans-serif;
        margin: 0;
        padding: 0;
      }
      .container {
        background: #fff;
        max-width: 480px;
        margin: 40px auto;
        border-radius: 12px;
        box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
        padding: 32px 28px 24px 28px;
      }
      .logo {
        display: block;
        margin: 0 auto 18px auto;
        width: 48px;
        height: 48px;
        border-radius: 12px;
        background: #007bff;
        color: #fff;
        font-size: 2rem;
        text-align: center;
        line-height: 48px;
        font-weight: bold;
        letter-spacing: 2px;
      }
      h2 {
        color: #22223b;
        text-align: center;
        margin-bottom: 12px;
      }
      p {
        color: #4a4e69;
        font-size: 1rem;
        margin: 16px 0;
        line-height: 1.6;
      }
      .btn-link {
        display: block;
        width: fit-content;
        margin: 28px auto 24px auto;
        padding: 12px 32px;
        background: linear-gradient(
          90deg,
          #033060 60%,
          #4f8cff 100%
        );
        color: #fff !important;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 600;
        font-size: 1.05rem;
        box-shadow: 0 2px 8px rgba(0, 123, 255, 0.08);
        transition: background 0.2s;
      }
      .btn-link:hover {
        background: linear-gradient(
          90deg,
          #0056b3 60%,
          #4f8cff 100%
        );
      }
      .footer {
        font-size: 0.85rem;
        color: #9a9a9a;
        text-align: center;
        margin-top: 32px;
      }
      .alt-link {
        word-break: break-all;
        color: #007bff;
        font-size: 0.95rem;
      }

    </style>
  </head>
  <body>
    <div class='container'>
      <div class='logo'>
        L
      </div>
      <h2>
        {{subject}}
      </h2>
      <p>
        Hello
        <strong>
          {{name}}
        </strong>
        ,
      </p>
      <p>
        Thank you for registering! Please click the button
        below to verify your account.
        <br />
        <span style='color:#007bff;'>
          This link is valid for 24 hours.
        </span>
      </p>
      <a href='{{link}}' class='btn-link'>
        Verify Account
      </a>
      <p style='margin-bottom: 24px;'>
        If you didn't create an account, you can safely
        ignore this email.
      </p>
      <hr
        style='border:none;border-top:1px solid #ececec;margin:24px 0;'
      />
      <div class='footer'>
        If you’re having trouble clicking the "Verify
        Account" button, copy and paste the URL below into
        your web browser:
        <br />
        <a href='{{link}}' class='alt-link'>
          {{link}}
        </a>
      </div>
    </div>
  </body>
</html>